name: 0_Analyze project code
on:
  workflow_dispatch:
  pull_request:
    branches: [ main, dev, release ]

jobs:
  analyze:
    runs-on: [ self-hosted, checker ]
    steps:
      - uses: actions/checkout@v3
      - name: Setup
        uses: ./.github/workflows/_setup_flutter.yaml
      - name: Check
        uses: ./.github/workflows/_check.yaml