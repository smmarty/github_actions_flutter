name: 0_Analyze project code
on:
  workflow_dispatch:
  pull_request:
    branches: [ main, dev, release ]

jobs:
  analyze:
    runs-on: [ self-hosted, checker ]
    steps:
      - uses: actions/checkout@v3
      - name: Setup
        uses: ./.github/actions/check-action.yml
      - name: Check
        uses: ./.github/actions/setup-action.yml